---
default: minor
---

# macOS Intel binary builds

Pre-built binaries for `x86_64-apple-darwin` (macOS Intel) are now included in
each GitHub Release, cross-compiled from the ARM64 runner using Apple's clang.
