---
default: patch
---

# Cleaner nightly version strings

Nightly builds now report `nightly.YYYYMMDD.SHORTSHA` from `--version` instead of the previous `0.0.0-nightly.YYYYMMDD.SHORTSHA`. Nightly artifact filenames follow the same format.
