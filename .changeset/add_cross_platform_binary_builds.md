---
default: minor
---

# Cross-platform binary builds

Pre-built binaries for `aarch64-apple-darwin`, `x86_64-unknown-linux-musl`,
`aarch64-unknown-linux-musl`, and `x86_64-pc-windows-msvc` are now attached
to each GitHub Release as assets.
