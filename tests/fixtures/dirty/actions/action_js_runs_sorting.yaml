name: JavaScript Action
description: Tests JavaScript runs key ordering
inputs:
  greeting:
    description: Who to greet
    required: true
runs:
  main: dist/index.js
  post: dist/cleanup.js
  using: node20
  pre: dist/setup.js
  post-if: always()
  pre-if: runner.os == 'Linux'