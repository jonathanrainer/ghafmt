name: Composite Steps

description: Tests step key and with sorting

runs:
  using: composite
  steps:
    - name: Build
      id: build_step
      if: always()
      run: echo "build"
      env:
        FOO: bar

    - name: Checkout
      id: checkout_step
      uses: actions/checkout@v4
      with:
        depth: 1
        token: ${{ inputs.token }}
